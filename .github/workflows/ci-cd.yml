name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build with Maven
        working-directory: C:\Users\MSI\Downloads\konyvek_beadando\pom.xml
        run: mvn clean install
